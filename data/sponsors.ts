// Signed sponsor logos, editable at /admin — see data/sponsors.json. Starts
// empty; the /sponsors wall fills remaining slots with "your logo here"
// placeholders until real sponsors are added.
import sponsorsData from "./sponsors.json";

export const SPONSORS: { name: string; logo: string }[] = sponsorsData.SPONSORS;
